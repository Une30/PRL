<!DOCTYPE html>
<html>
  <head>
    <title>PRL</title>
      <link href='http://fonts.googleapis.com/css?family=Ropa+Sans' rel='stylesheet' type='text/css'>
  </head>
  <body>
       <div class="header-nav">
        <div class="wrap">
          <div class="left-nav">
            <ul>
              <li><%= link_to "Home", {:controller => "home"} %></li>
                <li><%= link_to "Researches", researches_path %></li>
                <li><%= link_to "Publications", publications_path %></li>
                <li><%= link_to "Group", profiles_path %></li>
                <li><%= link_to "Alumni", {:controller => "alumni"} %></li>
                <li><%= link_to "Gallery", albums_path %></li>
                <li><%= link_to "Contact", {:controller => "contact"} %></li>
              </ul>
            </div>
            <div class="right-social-icons">
              <ul>
                <%= render 'devise/menu/registration_items' %>
                <%= render 'devise/menu/login_items' %>
              </ul>
            </div>
            <div class="clear"> </div>
        </div>
      </div>
    <div class="wrap">
      <div class="content">
        <h1>Listing abstract_files</h1>
        <table>
          <thead>
            <tr>
              <th>Project</th>
              <th colspan="3"></th>
            </tr>
          </thead>
          <tbody>
            <% @abstract_files.each do |abstract_file| %>
              <tr>
                <td><%= abstract_file.project_id %></td>
                <td><%= link_to 'Show', abstract_file %></td>
                <td><%= link_to 'Edit', edit_abstract_file_path(abstract_file) %></td>
                <td><%= link_to 'Destroy', abstract_file, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <br>
        <%if current_user and current_user.profile.isAdmin%>
          <%= link_to 'New Abstract file', new_abstract_file_path %>
        <%end%>
      </div>
    </div>
  </body>
</html>
